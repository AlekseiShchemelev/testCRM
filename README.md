# 🏠 RealtorCRM - Обучающий проект React + TypeScript

![React](https://img.shields.io/badge/React-19.1.1-blue?logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5.8.3-blue?logo=typescript)
![Vite](https://img.shields.io/badge/Vite-7.1.7-yellow?logo=vite)
![Firebase](https://img.shields.io/badge/Firebase-12.3.0-orange?logo=firebase)
![Material UI](https://img.shields.io/badge/Material%20UI-7.3.2-blue?logo=mui)
![PWA](https://img.shields.io/badge/PWA-Ready-green?logo=pwa)

**🌐 Демо:** [Перейти к приложению](https://alekseishchemelev.github.io/realtorCRM)

**🆕 НОВИНКА:** Теперь все изображения автоматически сжимаются при загрузке!

### 📸 Автоматическое сжатие изображений

Приложение теперь автоматически оптимизирует все загружаемые изображения:

- **🖼️ Автосжатие**: Все фото объектов автоматически сжимаются до 1920×1080px
- **📊 Качество**: Оптимизация с сохранением качества 80%
- **💾 Экономия**: Сокращение размера файлов на 70-90%
- **⚡ Скорость**: Ускоренная загрузка страниц
- **📈 Статистика**: Показ экономии места в интерфейсе

**Как это работает:**

- При добавлении/редактировании клиента
- Загруженные фотографии автоматически обрабатываются
- Показана статистика сжатия для каждого фото
- Сжатые изображения сохраняются в том же формате (JPEG)

**Технические детали:**

- Использует Canvas API для клиентского сжатия
- Поддерживает форматы: JPEG, PNG, WebP
- Максимум 10 фото на клиента
- Лимит 800KB на файл после сжатия

---

## 📚 Что это за проект?

**RealtorCRM** - это полнофункциональное CRM-приложение для риелторов, разработанное как **обучающий проект** для изучения современной разработки на React и TypeScript.

> 💡 **Для кого этот проект?**
>
> - Начинающие React/TypeScript разработчики
> - Студенты, изучающие веб-разработку
> - Разработчики, желающие изучить современные технологии
> - Все, кто хочет понять архитектуру реальных приложений

## 🎯 Обучающие цели проекта

Изучив этот проект, вы научитесь:

### 🚀 React и современная разработка

- ✅ **React Hooks**: useState, useEffect, useCallback, useRef, useMemo
- ✅ **Управление состоянием**: Context API, пользовательские хуки
- ✅ **Роутинг**: React Router v7 с защищенными маршрутами
- ✅ **Оптимизация**: Lazy loading, предзагрузка, React.memo
- ✅ **Обработка форм**: Валидация, управляемые компоненты
- ✅ **Асинхронность**: async/await, обработка ошибок

### 🎨 UI/UX и стилизация

- ✅ **Material UI v7**: Компоненты, темизация, responsive дизайн
- ✅ **CSS-in-JS**: Emotion для динамических стилей
- ✅ **Адаптивный дизайн**: Mobile-first подход
- ✅ **Анимации и переходы**: Плавные пользовательские взаимодействия

### 🔧 TypeScript и типизация

- ✅ **Типизация props**: Интерфейсы и типы
- ✅ **API типизация**: Типы для Firebase и других сервисов
- ✅ **Дженерики**: Создание переиспользуемых компонентов
- ✅ **Utility Types**: Pick, Partial, Record и другие

### 🔥 Firebase и Backend

- ✅ **Аутентификация**: Google OAuth интеграция
- ✅ **Firestore Database**: NoSQL база данных, коллекции, документы
- ✅ **Безопасность**: Правила безопасности, изоляция данных
- ✅ **Real-time updates**: Синхронизация данных в реальном времени

### 📱 Progressive Web App (PWA)

- ✅ **Service Workers**: Кеширование, работа офлайн
- ✅ **Web App Manifest**: Установка как нативное приложение
- ✅ **Push уведомления**: Browser Notifications API

### 🎤 Web APIs и интеграции

- ✅ **Speech Recognition**: Web Speech API для голосового ввода
- ✅ **File API**: Загрузка и обработка файлов
- ✅ **Maps API**: Интеграция с Яндекс.Картами

### 📅 Планирование встреч

- ✅ **Интерактивный календарь** с месячным видом
- ✅ **Цветовая индикация статуса** встреч (запланирована/состоялась/отменена)
- ✅ **Детальная информация** о каждой встрече
- ✅ **Быстрое изменение статуса** встреч

### 📸 Управление изображениями

- ✅ **Автоматическое сжатие** загружаемых фотографий (1920×1080px, 80% качество)
- ✅ **Статистика оптимизации** с показом экономии места
- ✅ **Галерея фотографий** с полноэкранным просмотром
- ✅ **Поддержка множественных фото** (до 10 на объект)
- ✅ **Быстрая загрузка** страниц за счет оптимизации изображений

### 🎤 Удобство ввода данных

- ✅ **Голосовой ввод** для всех текстовых полей
- ✅ **Автоматическое распознавание речи** на русском языке
- ✅ **Адаптивный интерфейс** для мобильных устройств

### 📊 Аналитика и история

- ✅ **Подробная история действий** пользователя
- ✅ **Экспорт данных** в Excel для аналитики
- ✅ **Отслеживание изменений** по каждому клиенту

### 🔐 Безопасность

- ✅ **Авторизация через Google** аккаунт
- ✅ **Изоляция данных** по пользователям
- ✅ **Облачное хранение** в Firebase Firestore

### 📱 Progressive Web App (PWA)

- ✅ **Установка на устройство** как нативное приложение
- ✅ **Работа офлайн** с кешированием данных
- ✅ **Push-уведомления** о встречах и событиях
- ✅ **Адаптивный интерфейс** для всех устройств

## 🏗 Архитектура приложения

### 📊 Диаграмма архитектуры

```
┌─────────────────────────────────────────────┐
│                 Frontend                     │
├─────────────────────────────────────────────┤
│  React 19 + TypeScript + Vite + Material UI │
├─────────────────────────────────────────────┤
│  📁 src/                                     │
│  ├── components/     # UI компоненты         │
│  ├── pages/          # Страницы приложения   │
│  ├── hooks/          # Пользовательские хуки │
│  ├── services/       # API и бизнес-логика   │
│  ├── types/          # TypeScript типы       │
│  ├── utils/          # Вспомогательные ф-ии  │
│  └── styles/         # Стили                │
└─────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────┐
│              Firebase                        │
├─────────────────────────────────────────────┤
│  🔐 Auth (Google OAuth)                     │
│  📊 Firestore (NoSQL Database)              │
│  📁 Storage (File Upload)                   │
│  🔔 Cloud Messaging (Push Notifications)    │
└─────────────────────────────────────────────┘
```

### 🎯 Принципы архитектуры

#### 1. **Компонентный подход**

```typescript
// Структура компонента
components/
├── ComponentName.tsx      # Основной компонент
├── ComponentName.test.tsx # Тесты
└── ComponentName.stories.tsx # Storybook stories
```

#### 2. **Разделение ответственности**

- **Components**: UI и отображение данных
- **Services**: Бизнес-логика и API вызовы
- **Hooks**: Переиспользуемая логика состояния
- **Types**: TypeScript интерфейсы и типы
- **Utils**: Вспомогательные функции

#### 3. **Паттерны React**

**🏠 Container/Presentational Pattern**

```typescript
// Container Component (умный)
const ClientsContainer = () => {
  const [clients, setClients] = useState([]);
  // Бизнес-логика здесь
  return <ClientsList clients={clients} />;
};

// Presentational Component (глупый)
const ClientsList = ({ clients }: { clients: Client[] }) => {
  return clients.map((client) => (
    <ClientCard key={client.id} client={client} />
  ));
};
```

**🎣 Custom Hooks Pattern**

```typescript
// Переиспользуемая логика
const useClients = () => {
  const [clients, setClients] = useState<Client[]>([]);

  const loadClients = async () => {
    const data = await clientService.getClients();
    setClients(data);
  };

  return { clients, loadClients };
};
```

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 16+ и npm
- Google аккаунт для Firebase аутентификации

### Установка и запуск

1. **Клонирование репозитория**

```bash
git clone <repository-url>
cd RealtorCRM
```

2. **Установка зависимостей**

```bash
npm install
```

3. **Настройка Firebase** (создайте проект на [console.firebase.google.com](https://console.firebase.google.com))

   - Включите Authentication с провайдером Google
   - Создайте Firestore Database
   - Получите конфигурацию и добавьте в `src/services/firebase.ts`

4. **Запуск в режиме разработки**

```bash
npm run dev
```

5. **Сборка для продакшена**

```bash
npm run build
```

## 📁 Структура проекта

```
src/
├── components/           # Переиспользуемые React компоненты
│   ├── ActionMenu.tsx        # Контекстное меню действий
│   ├── AddClientForm.tsx     # Форма добавления/редактирования клиента
│   ├── ClientCard.tsx        # Карточка клиента
│   ├── ConfirmDeleteDialog.tsx # Диалог подтверждения удаления
│   ├── ConfirmDialog.tsx     # Универсальный диалог подтверждения
│   ├── Header.tsx            # Шапка приложения
│   ├── PropertyGallery.tsx   # Галерея фотографий объектов
│   ├── ProtectedRoute.tsx    # Защищенные маршруты
│   ├── VoiceTextField.tsx    # Поле с голосовым вводом
│   └── ...
├── pages/               # Страницы приложения
│   ├── CalendarPage.tsx      # Календарь встреч
│   ├── ClientsPage.tsx       # Список клиентов
│   ├── HistoryPage.tsx       # История действий
│   ├── LoginPage.tsx         # Страница входа
│   └── ProfilePage.tsx       # Профиль пользователя
├── services/            # Сервисы и API
│   ├── clientService.ts      # Операции с клиентами
│   ├── dataService.ts        # Общий сервис данных
│   ├── historyService.ts     # История действий
│   └── firebase.ts           # Firebase конфигурация
├── hooks/               # Пользовательские React хуки
│   ├── useLongPress.ts       # Хук для обработки долгого нажатия
│   ├── useMeetingNotifications.ts # Уведомления о встречах
│   ├── useNotifications.ts   # Система уведомлений
│   └── usePreloadComponents.ts # Предзагрузка компонентов
├── types/               # TypeScript типы
│   └── index.ts             # Основные типы приложения
├── utils/               # Вспомогательные функции
│   ├── speechUtils.ts       # Утилиты для работы с речью
│   └── voiceParser.ts       # Парсер голосового ввода
├── styles/              # Стили
│   └── global.css           # Глобальные стили
├── App.tsx              # Главный компонент приложения
├── main.tsx             # Точка входа
├── sw.ts                # Service Worker для PWA
└── index.css            # Базовые стили
```

## 📖 Подробное руководство по изучению

### 🎓 Новые возможности: Подробные комментарии к коду!

**Все ключевые файлы проекта теперь содержат подробные комментарии**, которые объясняют:

- **Архитектурные решения** и почему они приняты
- **React паттерны** и их применение
- **Material UI компоненты** и их использование
- **TypeScript типы** и их роль в приложении
- **Оптимизации производительности** и как они работают
- **Responsive дизайн** и адаптация под устройства

### 🎯 Уровень 1: Основы React

#### 📝 Изучите эти файлы по порядку:

1. **`src/main.tsx`** - Точка входа в приложение
2. **`src/App.tsx`** - Главный компонент приложения
3. **`src/types/index.ts`** - Типы TypeScript
4. **`src/components/Header.tsx`** - Простой компонент

#### 🔍 Что изучить в каждом файле:

#### `src/main.tsx` - Bootstrap приложения

```typescript
// Изучите:
- createRoot() API новой версии React
- StrictMode для разработки
- Импорты CSS файлов
- DOM маунтинг
- TypeScript non-null assertion (!)
- React 18+ Concurrent Features
```

#### `src/App.tsx` - Архитектура приложения

```typescript
// Изучите:
- React Router настройка
- Lazy loading компонентов
- Suspense для состояния загрузки
- Глобальные хуки (useMeetingNotifications)
- Адаптивный дизайн с useMediaQuery
- Предзагрузка компонентов
- Защищенные маршруты
```

### 🎯 Уровень 2: Состояние и хуки

#### 📝 Следующие файлы для изучения:

1. **`src/hooks/useNotifications.tsx`** - Система уведомлений
2. **`src/hooks/usePreloadComponents.tsx`** - Оптимизация производительности
3. **`src/pages/ClientsPage.tsx`** - Работа с состоянием

#### 🔍 Ключевые концепции:

**useState - Локальное состояние:**

```typescript
const [clients, setClients] = useState<Client[]>([]);
const [loading, setLoading] = useState(false);
```

**useEffect - Побочные эффекты:**

```typescript
useEffect(() => {
  loadClients();
}, []); // Пустой массив = выполнить один раз при маунте
```

**useCallback - Оптимизация функций:**

```typescript
const handleDelete = useCallback((id: string) => {
  // Логика удаления
}, []); // Зависимости указываются здесь
```

### 🎯 Уровень 3: Формы и валидация

#### 📝 Изучите:

1. **`src/components/AddClientForm.tsx`** - Комплексная форма
2. **`src/components/VoiceTextField.tsx`** - Продвинутое поле ввода
3. **`src/services/clientService.ts`** - API логика

#### 🔍 Ключевые концепции:

**Управляемые компоненты:**

```typescript
const [formData, setFormData] = useState({
  fullName: "",
  phone: "",
  address: "",
});

const handleChange = (e) => {
  setFormData((prev) => ({
    ...prev,
    [e.target.name]: e.target.value,
  }));
};
```

**Валидация данных:**

```typescript
const validateForm = () => {
  const errors = [];
  if (!formData.fullName?.trim()) {
    errors.push("ФИО обязательно");
  }
  return errors;
};
```

### 🎯 Уровень 4: API и асинхронность

#### 📝 Изучите:

1. **`src/services/firebase.ts`** - Firebase конфигурация
2. **`src/services/clientService.ts`** - CRUD операции
3. **`src/services/historyService.ts`** - История действий

#### 🔍 Ключевые концепции:

**async/await паттерн:**

```typescript
const loadClients = async () => {
  try {
    setLoading(true);
    const data = await getClients();
    setClients(data);
  } catch (error) {
    showError("Ошибка загрузки");
  } finally {
    setLoading(false);
  }
};
```

**Обработка ошибок:**

```typescript
try {
  await addClient(clientData);
} catch (error: any) {
  showError(error.message || "Не удалось добавить клиента");
}
```

### 🎯 Уровень 5: Продвинутые паттерны и компоненты

#### 📝 Изучите:

1. **`src/components/ClientCard.tsx`** - Карточка клиента
2. **`src/components/Header.tsx`** - Навигация и адаптивный дизайн
3. **`src/utils/speechUtils.ts`** - Web APIs
4. **`src/hooks/useMeetingNotifications.ts`** - Push уведомления

#### 🔍 Ключевые концепции:

**Presentational Components:**

```typescript
// Компонент только отображает данные
// Вся логика передается через props
// Переиспользуемый и тестируемый
const ClientCard = ({ client, onEdit, onDelete }) => {
  return (
    <Card>
      <Typography>{client.fullName}</Typography>
      {/* ... */}
    </Card>
  );
};
```

**Responsive Design с Material UI:**

```typescript
sx={{
  display: "flex",
  flexDirection: { xs: "column", sm: "row" }, // Адаптивность
  gap: { xs: 1, sm: 2 }, // Разные отступы
  fontSize: { xs: "0.875rem", sm: "1rem" }, // Размеры шрифта
}}
```

**Custom Hooks Pattern:**

```typescript
export const useNotifications = () => {
  const [notifications, setNotifications] = useState([]);

  const showNotification = (message, variant) => {
    // Логика показа уведомления
  };

  return { notifications, showNotification };
};
```

### 🎯 Уровень 5: Продвинутые паттерны

#### 📝 Изучите:

1. **`src/components/PropertyGallery.tsx`** - Комплексный компонент
2. **`src/utils/speechUtils.ts`** - Web APIs
3. **`src/hooks/useMeetingNotifications.ts`** - Push уведомления

#### 🔍 Ключевые концепции:

**Пользовательские хуки:**

```typescript
export const useNotifications = () => {
  const [notifications, setNotifications] = useState([]);

  const showNotification = (message: string, variant: string) => {
    // Логика показа уведомления
  };

  return { notifications, showNotification };
};
```

**Web Speech API:**

```typescript
export const startSpeechRecognition = () => {
  return new Promise((resolve, reject) => {
    const recognition = new webkitSpeechRecognition();
    recognition.onresult = (event) => {
      resolve(event.results[0][0].transcript);
    };
    recognition.start();
  });
};
```

## 🚀 Практические задания

### 📚 Как использовать комментарии для изучения

**Все ключевые файлы проекта теперь содержат подробные комментарии**, которые объясняют архитектуру, паттерны и решения:

#### 🗂️ Прокомментированные файлы:

**Основа приложения:**

- **`src/main.tsx`** - Bootstrap React приложения, createRoot API, StrictMode
- **`src/App.tsx`** - Архитектура приложения, роутинг, lazy loading, Suspense
- **`src/types/index.ts`** - TypeScript интерфейсы и их роль в приложении

**Сервисы и API:**

- **`src/services/clientService.ts`** - CRUD операции с Firebase, валидация, обработка ошибок
- **`src/services/firebase.ts`** - Настройка Firebase, переменные окружения

**Хуки и состояние:**

- **`src/hooks/useNotifications.tsx`** - Система уведомлений без Context API, глобальное состояние

**Компоненты:**

- **`src/components/ClientCard.tsx`** - Карточка клиента, responsive дизайн, Material UI

#### 💡 Что объясняют комментарии:

```typescript
/**
 * Архитектурные решения:
 * - Почему выбран именно такой подход
 * - Альтернативные варианты и их недостатки
 * - Паттерны проектирования (Presentational/Container, Custom Hooks)
 *
 * React концепции:
 * - useState, useEffect, useCallback, useMemo
 * - Props drilling и способы избежать
 * - Conditional rendering и списки
 *
 * Material UI:
 * - Компоненты и их использование
 * - Responsive дизайн с sx prop
 * - Темизация и кастомизация
 *
 * TypeScript:
 * - Интерфейсы и типы
 * - Generics и utility types
 * - Безопасность типов
 *
 * Performance:
 * - Lazy loading и code splitting
 * - Мемоизация компонентов и функций
 * - Оптимизация ре-рендеров
 */
```

#### 🎯 Советы по изучению:

1. **Читайте комментарии сверху вниз** - они написаны в логическом порядке
2. **Сравнивайте похожие файлы** - например, ClientCard и потенциальный StatsCard
3. **Экспериментируйте с кодом** - изменяйте что-то и смотрите результат
4. **Задавайте вопросы** - если что-то непонятно, исследуйте дополнительно
5. **Практикуйтесь** - используйте изученные паттерны в своих заданиях

#### 🔍 Как читать комментарии:

```typescript
// ❌ Плохо: Комментарий только что делает код
const [clients, setClients] = useState<Client[]>([]);

// ✅ Хорошо: Комментарий объясняет зачем и почему
// Локальное состояние для хранения списка клиентов
// Используем useState так как данные специфичны для этого компонента
// TypeScript обеспечивает типобезопасность
const [clients, setClients] = useState<Client[]>([]);
```

### 🎯 Задание 1: Создайте новую страницу

**Цель**: Добавить страницу "Настройки"

1. Создайте `src/pages/SettingsPage.tsx`
2. Добавьте роут в `App.tsx`
3. Создайте сервис `src/services/settingsService.ts`
4. Добавьте ссылку в Header

**Изучаемые концепции:**

- Роутинг в React
- Создание новых страниц
- Сервисы и API
- Навигация

**💡 Подсказка из кода**: Изучите `App.tsx` для понимания роутинга и `ClientCard.tsx` для структуры компонентов

### 🎯 Задание 2: Расширьте систему уведомлений

**Цель**: Добавить новый тип уведомлений

1. Добавьте `success` тип в `useNotifications.tsx`
2. Используйте в любом компоненте
3. Создайте компонент для уведомлений

**Изучаемые концепции:**

- Пользовательские хуки
- TypeScript дженерики
- React Context
- Глобальное состояние без Context

**💡 Подсказка из кода**: Изучите `useNotifications.tsx` - там подробно объяснена архитектура системы уведомлений

### 🎯 Задание 3: Добавьте фильтрацию клиентов

**Цель**: Фильтрация по статусу встречи

1. Добавьте фильтр в `ClientsPage.tsx`
2. Обновите интерфейс `Client`
3. Создайте компонент `StatusFilter`

**Изучаемые концепции:**

- Состояние и фильтрация
- TypeScript интерфейсы
- Композиция компонентов
- useState и useEffect

**💡 Подсказка из кода**: `ClientsPage.tsx` показывает как реализовать поиск и фильтрацию с подробными комментариями

### 🎯 Задание 4: Интеграция с внешним API

**Цель**: Добавьте получение данных о недвижимости

1. Создайте сервис `realEstateApi.ts`
2. Добавьте поле в `Client` интерфейс
3. Интегрируйте в форму

**Изучаемые концепции:**

- Внешние API
- Асинхронность
- Обработка ошибок
- Firebase vs внешние API

**💡 Подсказка из кода**: `clientService.ts` показывает как работать с API, включая обработку ошибок и валидацию

### 🎯 Задание 5: Создайте новый компонент

**Цель**: Компонент для отображения статистики

1. Создайте `src/components/StatsCard.tsx`
2. Добавьте на главную страницу
3. Покажите количество клиентов, встреч и т.д.

**Изучаемые концепции:**

- Создание переиспользуемых компонентов
- Material UI компоненты
- Responsive дизайн
- Props и их типизация

**💡 Подсказка из кода**: `ClientCard.tsx` - отличный пример создания красивого и функционального компонента

### 🎯 Бонусное задание: Оптимизация производительности

**Цель**: Улучшите производительность приложения

1. Добавьте React.memo в компоненты
2. Оптимизируйте ре-рендеры с useCallback
3. Реализуйте виртуализацию для больших списков

**Изучаемые концепции:**

- React.memo
- useCallback и useMemo
- Performance profiling
- Виртуализация списков

**💡 Подсказка из кода**: В `App.tsx` изучите lazy loading, а в `useNotifications.tsx` - паттерн глобального состояния

## 🔧 Настройка среды разработки

### Предварительные требования

- **Node.js 16+** и npm/yarn
- **Git** для версионирования
- **Google аккаунт** для Firebase

### Установка проекта

```bash
# 1. Клонирование
git clone https://github.com/alekseishchemelev/realtorCRM.git
cd realtorCRM

# 2. Установка зависимостей
npm install

# 3. Настройка Firebase
# Создайте проект на https://console.firebase.google.com
# Включите Authentication (Google) и Firestore
# Скопируйте конфигурацию в src/services/firebase.ts

# 4. Запуск
npm run dev
```

### 🔍 Полезные команды

```bash
npm run dev          # Запуск в development
npm run build        # Сборка для production
npm run lint         # Проверка кода ESLint
npm run preview      # Предпросмотр build
npm run deploy       # Деплой на GitHub Pages
```

### 🎓 Следующие шаги после изучения

1. **Изучите тестирование**: Jest, React Testing Library
2. **State Management**: Redux Toolkit, Zustand
3. **Server-side Rendering**: Next.js, Remix
4. **Mobile Development**: React Native, Expo
5. **Backend**: Node.js, Express, NestJS

## 📱 Мобильная адаптация и PWA

Приложение полностью адаптировано для работы на мобильных устройствах и может быть установлено как нативное приложение:

- 📱 **Responsive дизайн** с оптимизацией для маленьких экранов
- 👆 **Touch-friendly интерфейс** с увеличенными элементами управления
- 🔄 **Адаптивная сетка** календаря и списков
- 💫 **Плавные анимации** и переходы
- 📲 **PWA установка** - добавьте на главный экран устройства
- 🔄 **Работа офлайн** - доступ к кешированным данным без интернета
- 🔔 **Push-уведомления** о встречах и важных событиях

## 🎨 Особенности UI/UX

- **Material Design**: Современный дизайн на основе Material UI 7.x
- **Темная тема**: Поддержка системной темы
- **Анимации**: Плавные переходы между состояниями
- **Уведомления**: Система toast-уведомлений для обратной связи
- **Валидация в реальном времени**: Мгновенная проверка полей формы
- **PWA интерфейс**: Нативное поведение на всех устройствах
- **Оптимизация производительности**: Предзагрузка и ленивая загрузка компонентов

## 🚀 Развертывание

### GitHub Pages (текущий хостинг)

Проект автоматически развернут на GitHub Pages:
**🌐 [Перейти к приложению](https://alekseishchemelev.github.io/realtorCRM)**

```bash
# Автоматическое развертывание через GitHub Actions
npm run deploy
```

### Vercel (рекомендуется для разработки)

```bash
npm install -g vercel
vercel
```

### Netlify

```bash
npm run build
# Загрузите папку dist/ на Netlify
```

### Firebase Hosting

```bash
npm install -g firebase-tools
firebase login
firebase init hosting
firebase deploy
```

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 📞 Поддержка

Если у вас есть вопросы или предложения:

- 🐛 [Создайте Issue](issues)
- 💬 Обсуждение в [Discussions](discussions)

---

**Разработано для профессиональных риелторов** 🏠✨
